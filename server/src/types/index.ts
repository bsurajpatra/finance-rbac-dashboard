/**
 * Barrel Export for Types
 * This simplifies imports from 'src/types' by allowing controllers and models 
 * to import everything from a single index file rather than individual internal files.
 * 
 * Target Usage:
 * import { Role } from '../types';
 */

export * from './role.enum';
